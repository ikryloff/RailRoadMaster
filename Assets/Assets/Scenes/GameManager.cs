using UnityEngine;
using UnityEngine.UI;

public class GameManager : Singleton<GameManager> { 
              
    void Start () {
       // velocity.text = "Speed: " + car.velocity.magnitude;
        //control.text = "Position: " + controllerPosition;
        //isBrakes.text = "Use Brakes: " + (brakes ? "yes" : "no");
     
    }
	
	void Update ()
    {
        //velocity.text = "Speed: " + car.velocity.magnitude;
        //control.text = "Position: " + controllerPosition;
        //isBrakes.text = "Use Brakes: " + (brakes ? "yes" : "no");

    }    

}
